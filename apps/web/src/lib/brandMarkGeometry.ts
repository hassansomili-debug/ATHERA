/**
 * هندسةُ علامة بُبريفا — **المصدرُ الوحيد للحقيقة**.
 *
 * الصيغُ الأربع والمكوّن كلُّها تُشتقّ من هذا الملفّ. ولو كُتبت الهندسةُ
 * في كلٍّ منها على حدة لاتّفقت اليوم وافترقت بعد شهر، ولَما شكا أحد:
 * صيغةٌ تُعدَّل لأنها بدت ثقيلةً في موضعها، فتصير عندنا علامتان تتشابهان
 * ولا تتطابقان — وذاك ما لا تلتقطه عينٌ تقارن بالذاكرة.
 *
 * **والعلامةُ خيطٌ مجرَّد لا حرفٌ مرسوم.**
 *
 * وقد رُسمت أوّلَ مرّة حرفَ `P` صريحًا: ساقٌ رأسيّة طويلة (`M16 40V9`)
 * وقاعدةٌ أفقيّة تحتها (`M16 40h16`) وأربعُ عقد. وذلك ليس ما اعتمده
 * صاحبُ المنتج. المعتمَدُ شكلٌ منحنٍ أفقيُّ الميل، يبدأ من عقدةٍ بنفسجيّة
 * ويلتفّ صاعدًا إلى اليمين ثمّ عائدًا، فينحدر في فرعٍ سفليٍّ ينتهي بعقدةٍ
 * فيروزيّة — و**الحرفُ يظهر من المسار المتّصل لا يُرسم**.
 *
 * ثلاثُ عقدٍ رئيسة، ولا ساق، ولا قاعدة.
 */

/** إطارُ الرسم — واحدٌ في كل الصيغ، فلا تتمطّط العلامة في موضعٍ دون آخر. */
export const VIEW_BOX = "0 0 48 48";

/**
 * الخيط: مسارٌ **واحد** لا ينقطع.
 *
 * يبدأ يسارًا عند عقدة البداية، يصعد ملتفًّا إلى اليمين، يعود منحنيًا إلى
 * الداخل، ثمّ يهبط في فرعٍ سفليٍّ إلى عقدة النهاية. واتّصالُه هو الدعوى
 * نفسها: البحث خطٌّ واحد من الفكرة إلى النشر.
 */
export const THREAD_PATH =
  "M11.6 21.4C12.4 12.6 20.8 6.9 29.4 8.1C36.9 9.2 40.6 15.4 39.2 21.6" +
  "C37.8 27.9 30.4 31.1 25.2 28.9C20.6 27 17.2 31.2 16.6 37.2";

/** عرضُ الخيط — يبقى مقروءًا عند ستّة عشر بكسلًا كما هو عند مئةٍ وستّين. */
export const STROKE_WIDTH = 4.6;

/** رمزُ اللون في كل عقدة — يُقرأ من الطبقة الأولى، ولا يُكتب رقمًا هنا. */
export type NodeTone = "violet" | "teal";

export interface MarkNode {
  cx: number;
  cy: number;
  r: number;
  tone: NodeTone;
  /** لماذا هذه العقدة موجودة — يُقرأ في المراجعة لا في الشاشة. */
  meaning: string;
}

/**
 * العقدُ الثلاث: مواضعُ القرار على الخيط.
 *
 * ولا رابعة: العقدةُ الزائدة تصير نقطةً سائبة عند ستّة عشر بكسلًا، فتُقرأ
 * غبارًا لا معنًى.
 */
export const NODES: readonly MarkNode[] = [
  { cx: 11.6, cy: 21.4, r: 3.3, tone: "violet", meaning: "المبتدأ — سؤالٌ يُطرح" },
  { cx: 25.6, cy: 19.2, r: 3.7, tone: "teal", meaning: "الوسط — دليلٌ يُراجَع" },
  { cx: 16.6, cy: 37.4, r: 3.5, tone: "teal", meaning: "المنتهى — نشرٌ يقع" },
];

/** تدرّجُ الخيط: نيليٌّ من الأسفل إلى بنفسجيٍّ في الأعلى، كما في الورقة. */
export const GRADIENT = {
  x1: 10, y1: 38, x2: 40, y2: 8,
  from: "--brand-indigo", fromHex: "#4B46A9",
  to: "--brand-violet", toHex: "#7867F2",
} as const;

/** ألوانُ العقد حين لا تتوفّر الطبقة الأولى (ملفٌّ مستقلٌّ لا يرث متغيّرًا). */
export const TONE_HEX: Record<NodeTone, string> = {
  violet: "#7867F2",
  teal: "#17BEBB",
};
