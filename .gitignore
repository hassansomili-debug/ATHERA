# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
# Node
node_modules/
.next/
out/
# Env & secrets — never commit
.env
.env.*
!.env.example
*.pem
*.key
# Build
dist/
build/
*.egg-info/
# OS
.DS_Store
# نسخ iCloud المتضاربة: يُنشئها التزامن باسم "ملف 2.py" فتُجمَع مع
# الاختبارات وتُشغَّل نسخة قديمة، أو تُلتزم إلى git بلا انتباه.
* [0-9].*
*.icloud

# ناتج tsc التزايدي — يُولَّد عند كل `npm run typecheck`
*.tsbuildinfo

# بيئة افتراضية محلية للتحقق | local verification venv
.venv*

# أدواتُ الوكلاء المحلّية — لا تُدفع
.claude/

# آثارُ Playwright: لقطاتٌ وتتبُّعٌ وفيديو تُكتب عند سقوط فحص — مخرَجُ
# تشغيلٍ محلّيّ لا مصدرٌ يُراجَع. وكانت `test-results/` غيرَ مستثناة، فأيُّ
# تشغيلٍ فيه سقوطٌ يعرض آلافَ الملفّات على `git add .`.
apps/web/test-results/
apps/web/playwright-report/
apps/web/blob-report/
